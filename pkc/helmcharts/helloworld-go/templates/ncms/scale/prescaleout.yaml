
{{ if gt (int .Values.prescaleout ) 0 }}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ template "fullname" . }}-prescaleout"
spec:
  template:
    spec:
      securityContext:
{{ toYaml .Values.securityContext | indent 8 }}
      containers:
      - name: {{ template "fullname" . }}-prescaleout
        image: "{{ .Values.global.registry }}/{{ .Values.kubectlImage }}:{{ .Values.kubectlTag }}"
        imagePullPolicy: IfNotPresent
        resources:
{{ toYaml .Values.resources | indent 10 }}
        securityContext:
{{ toYaml .Values.containerSecurityContext | indent 10 }}
        command:
          - sh
          - "-c"
          - |
            echo "PreScaleOut hook!"
            sleep 2
      restartPolicy: Never
{{- end }}
